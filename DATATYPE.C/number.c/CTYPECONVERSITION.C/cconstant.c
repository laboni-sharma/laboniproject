#include <stdio.h>

int main() {
    const int minutesPerHour = 60;
    const int monthsInYear = 12;

    printf("Minutes in an hour: %d\n", minutesPerHour);
    printf("Months in a year: %d\n", monthsInYear);

    return 0;
}