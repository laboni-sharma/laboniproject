#include <stdio.stdio.h> // Header file for input/output functions

int main() {
    int i = 0; // Initialize loop counter

    // While loop execution
    while (i < 5) {
        printf("%d\n", i);
        i++; // Increment counter
    }

    return 0; // Standard exit code for success
}